/// <reference types="mongoose" />
import { Model } from 'mongoose';
export declare class MongoTestUtils {
    private static spawn;
    /**
     * Start the mongo deamon
     *
     * @param  {(err?)=>{}} done
     * @returns void
     */
    static startMongo(done: (err?) => {}): void;
    /**
     * Stop the mongo deamon
     *
     * @param  {(err?)=>{}} done
     * @returns void
     */
    static stopMongo(done: (err?) => {}): void;
    /**
     * Util to get the Mongoose Model
     * from the root module DI
     *
     * @param  {any} model
     * @returns Model
     */
    static getDefaultMongooseModel(model: any): Model<any>;
}
