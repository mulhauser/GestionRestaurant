import { Type } from '@hapiness/core';
import { MongoManager, ModelManager } from '../managers';
export interface ConnectionOptions {
    adapter: string;
    options?: any;
}
export declare class MongoClientService {
    private _mongoManager;
    constructor(_mongoManager: MongoManager);
    get(): MongoManager;
    getDao(connectionOptions: ConnectionOptions): any;
    getStore(connectionOptions: ConnectionOptions): ModelManager;
    getModel(connectionOptions: ConnectionOptions, token: Type<any>): any;
}
