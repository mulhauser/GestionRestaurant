import { HapinessMongoAdapter, HapinessMongoAdapterConstructorArgs } from '../adapters';
import { Observable } from 'rxjs/Observable';
export declare class MongoManager {
    private _config;
    private _adapters;
    private _adaptersInstances;
    constructor(config?: HapinessMongoAdapterConstructorArgs);
    private _fixConfig(configValues?);
    private _keyForAdapter(adapterName, options);
    registerAdapter(adapterClass: typeof HapinessMongoAdapter): boolean;
    loadAdapter(adapterName: string, options?: any): Observable<HapinessMongoAdapter>;
    getAdapter(adapterName: string, options?: any): HapinessMongoAdapter;
}
