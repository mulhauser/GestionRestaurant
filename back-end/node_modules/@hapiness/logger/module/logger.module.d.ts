import { CoreModuleWithProviders } from '@hapiness/core';
import { LoggerConfig } from './logger.config';
export declare class LoggerModule {
    static setConfig(config: LoggerConfig): CoreModuleWithProviders;
}
