import { Observable } from 'rxjs/Observable';
import { CoreModuleWithProviders, OnRegister, Server } from '@hapiness/core';
export declare class SwagModule implements OnRegister {
    private server;
    private config;
    static setConfig(config: any): CoreModuleWithProviders;
    constructor(server: Server, config: any);
    onRegister(): Observable<Error>;
}
